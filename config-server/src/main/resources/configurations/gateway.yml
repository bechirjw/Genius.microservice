eureka:
  client:
    register-with-eureka: false
server:
  port: 6002
spring:
  application:
    name: gateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      # routes:


